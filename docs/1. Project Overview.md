# cc-break-dead-loop 项目总览

## 项目是什么

`cc-break-dead-loop` 是一个 Claude Code 插件，自动检测并打断 agent 的两类死循环：

1. **主 agent Read 死循环**：主 agent 在 Read 工具返回 `"Wasted call — file unchanged since your last Read"` 或 `{ type: "file_unchanged" }` 时不理解信号，连续重复读取同一文件。
2. **子 agent 工具调用死循环**：子 agent（subagent / teammate）陷入连续重复调用同一工具（任意工具）。子 agent 无视 hook 的 `permissionDecision: 'deny'`（Claude Code 已知 bug #25000/#34692），常规 Hook 无法直接阻断。

## 核心目标

1. **双线检测**：线 1（主 agent Read，双 Hook）+ 线 2（子 agent 任意工具，watcher 常驻进程）
2. **渐进式干预**（线 1）：连续 3 次无效 Read 注入警告，5 次强制阻断
3. **绕过 subagent deny 失效**（线 2）：watcher 扫 subagent transcript 检测死循环，经 Stop hook `blockingError` 引导主 agent 调用 `TaskStopTool` 终止子 agent
4. **永不阻断正常操作**：多重错误边界确保插件 bug 不影响正常 Read / 不阻断 Claude Code 启动；watcher 崩溃自动重启
5. **隔离**：线 1 按 project + session + agent 隔离计数，线 2 按 sessionId 过滤告警

## 技术栈

| 维度 | 选择 |
|------|------|
| 语言 | JavaScript（ES Module，`.mjs`）|
| 运行时 | Node.js >= 18 |
| 测试框架 | Vitest ^4.1.8（devDependency），断言用 `node:assert` |
| 依赖 | **零运行时依赖**（Node.js 内置模块）；开发依赖 `vitest` |
| 构建 | **无构建步骤**，直接运行原生 JS |

## 核心特性

- **双线检测**：线 1（主 agent Read，双 Hook）+ 线 2（子 agent 工具死循环，watcher）
- **watcher 常驻进程**：detached spawn，心跳保活（30s 超时重启），崩溃自愈
- **绕过 subagent deny 失效**：子 agent 死循环由主 agent 经 `TaskStopTool` 终止（Stop hook `blockingError` + `exit 2`）
- **多重检测兼容**（线 1）：字符串 `"Wasted call"` / `{ type: "file_unchanged" }` 对象 / `content` 对象 / `JSON.stringify` 兜底
- **参数精确比较**（线 1）：`===` 直接比较，不规范化 `undefined` → `0`
- **稳定序列化指纹**（线 2）：对象键排序后 stringify，键序不同的等价参数视为相同
- **原子写入**：`state.json`、`alerts.json` 均 `writeFile(tmp) → rename(dest)`
- **多重错误边界**：handler try/catch + node-runner graceful fallback + watcher 崩溃自愈 + Setup 永不阻断

## 项目结构

```
cc-break-dead-loop/
├── package.json              # 项目元数据，type: "module"，vitest devDep
├── pnpm-lock.yaml            # pnpm lockfile
├── vitest.config.mjs         # Vitest 配置（include tests/**/*.test.mjs）
├── Agents.md                 # 代理指令入口（Claude.md 为其符号链接）
├── README.md                 # 安装与使用指南
├── TODOS.md                  # 未来改进事项
├── plugin/                   # Claude Code 插件目录
│   ├── .claude-plugin/
│   │   └── plugin.json       # 插件元数据
│   ├── hooks/
│   │   └── hooks.json        # Hook 注册（Setup + SessionStart + PostToolUse[Read+*] + PreToolUse[Read] + Stop）
│   ├── src/                  # 核心源码（13 模块）
│   │   ├── index.mjs               # Hook 入口：5 事件分发、watcher 保活接线、Stop 阻断、错误边界
│   │   ├── config.mjs              # 阈值 + 数据目录 + watcher 参数常量
│   │   ├── handlers.mjs            # 线 1：PostToolUse:Read + PreToolUse:Read
│   │   ├── state.mjs               # 线 1：主 agent 计数状态（原子写入）
│   │   ├── utils.mjs               # 路径安全化 + Git 仓库名解析
│   │   ├── watcher.mjs             # 线 2：扫描协调（transcript → 检测 → 告警同步）
│   │   ├── watcherLifecycle.mjs    # 线 2：watcher 进程决策与 spawn
│   │   ├── alertStore.mjs          # 线 2：告警存储（watcher 写 / hooks 读）
│   │   ├── deadLoopDetector.mjs    # 线 2：死循环检测算法（稳定序列化指纹）
│   │   ├── hookInjector.mjs        # 线 2：Hook 注入措辞生成
│   │   ├── subagentTranscriptReader.mjs # 线 2：subagent jsonl 解析（含尾块时间戳读取）
│   │   ├── notifier.mjs            # 桌面通知（活跃死循环提醒 + watcher 复活通知）
│   │   └── sessionStartAdvice.mjs  # SessionStart 注入文案（引导后台子代理）
│   └── scripts/
│       ├── node-runner.mjs   # Hook 运行时：stdin 收集、Stop exit 2、graceful fallback
│       ├── setup-check.mjs   # 环境检测 + 启动/保活 watcher
│       └── watcher.mjs       # watcher 常驻进程入口（detached spawn）
├── tests/                    # 测试套件（12 文件，136 用例，Vitest）
│   ├── state.test.mjs               # 状态管理（主 agent 计数）
│   ├── handlers.test.mjs            # Handler 逻辑（isWastedCall 多模式）
│   ├── integration.test.mjs         # 端到端 stdin/stdout 协议（env 隔离）
│   ├── watcher.test.mjs             # watcher 扫描协调（fake timers）
│   ├── watcherLifecycle.test.mjs    # 进程决策与 spawn（mock child_process）
│   ├── watcherKeepalive.test.mjs    # 保活接线集成（真实 spawn + PATH shim 通知断言）
│   ├── alertStore.test.mjs          # 告警读写与并发
│   ├── deadLoopDetector.test.mjs    # 检测算法（尾部连续重复、稳定序列化）
│   ├── hookInjector.test.mjs        # 注入措辞与最严重告警选取
│   ├── subagentTranscriptReader.test.mjs # jsonl 解析容错 + 尾块时间戳读取
│   ├── notifier.test.mjs            # 桌面通知平台分发（依赖注入）
│   └── sessionStartAdvice.test.mjs  # SessionStart 注入文案
└── docs/                     # 本文档目录
    ├── 1. Project Overview.md
    ├── 2. Architecture Overview.md
    ├── 3. Workflow Overview.md
    ├── 4.DeepDive/           # 各模块深入分析
    ├── plans/                # 计划文档（时间戳快照）
    └── brainstorms/          # 需求文档（时间戳快照）
```

## 快速开始

```bash
# 运行全部测试（136 用例，12 文件）
npm test                         # = vitest run

# watch 模式（TDD）
npm run test:watch

# 运行单个测试文件
npx vitest run tests/state.test.mjs
npx vitest run tests/watcher.test.mjs

# 按测试名过滤
npx vitest -t "sanitizeName"

# 手动模拟 Hook 输入
echo '{"tool_name":"Read","tool_input":{"file_path":"/a/b"},"tool_response":{"type":"file_unchanged","file":{"filePath":"/a/b"}},"session_id":"s","agent_id":"a","cwd":"/tmp"}' | node plugin/src/index.mjs post-tool-use
```

## 安装

```bash
# 在 Claude Code CLI 中
/plugin marketplace add Lionad-Morotar/claude-plugins
/plugin install cc-break-dead-loop@lionad-morotar
```

> 如果你之前按 `Lionad-Morotar/cc-break-dead-loop` 添加过 marketplace，请先执行 `/plugin marketplace remove lionad-morotar`，避免与中心集市的其他插件冲突。

### 验证安装

重启 Claude Code（新开会话即触发 SessionStart hook，watcher 自动拉起），随后确认常驻进程存活：

```bash
cat ~/.data/cc-break-dead-loop/watcher-heartbeat.json   # ts 应在最近 30 秒内（毫秒时间戳）
ps -p $(cat ~/.data/cc-break-dead-loop/watcher.pid)      # 应显示 watcher.mjs 进程
```

watcher 若中途死亡（睡眠 / OOM / 手动 kill），会在下一次 SessionStart、Stop 或任意工具调用 hook 时自愈重启并弹桌面通知，无需手动干预。

## 已知限制

- **线 2 依赖主 agent 遵循引导**：watcher 检测到子 agent 死循环后，通过 Stop hook `blockingError`（`exit 2`）+ PostToolUse `additionalContext` 引导主 agent 调用 `TaskStopTool`。若主 agent 忽略 `blockingError`，仍可能漏阻断。
- **watcher 检测有延迟**：扫描间隔 5s + transcript 落盘延迟，子 agent 死循环最快在数秒后被发现。
- **subagent/teammate 无视 `permissionDecision: 'deny'`**（Claude Code bugs #25000, #34692）：线 1 的 deny 对主 agent 完全有效，对子 agent 无效（故引入线 2 watcher 机制）。
- **独立 agent_id → 独立计数**（线 1）：不同 agent 各自维护计数器，互不影响。

## 相关文档

- [2. Architecture Overview](2.%20Architecture%20Overview.md) — 系统架构与 C4 模型
- [3. Workflow Overview](3.%20Workflow%20Overview.md) — 核心工作流与数据流
- [4. Deep Dive](4.DeepDive/) — 各模块深入分析
